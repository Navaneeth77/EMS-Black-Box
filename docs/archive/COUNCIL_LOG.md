# Council log

One row per cycle. MAKER implements, REVIEWER audits, DEBUGGER fixes, REVIEWER
re-checks. Kept terse on purpose; the reasoning lives in the code and in
`3D_ARCHITECTURE.md` / `FINAL_VALIDATION.md`.

| # | Role | Task | Findings | Fixes | Tests | Status |
|---|---|---|---|---|---|---|
| 1 | REVIEWER | Diagnose "only 1 of 8 TLS visible" | All 8 **are** exported and rendered. They span 3.7 km; 4 cluster within ~100 m near Silk Board, 2 are the large joined signals, 2 are isolated. A junction-level camera sees 1–4. **Not a rendering bug.** Separately found `12611782078` sits **outside** the declared study bbox (lon 77.6323 > 77.6306) | Added an all-TLS overview affordance (cycle 6) | n/a | ACCEPT |
| 2 | REVIEWER | Diagnose "scene almost empty" | Not empty: **165–198 concurrent vehicles per frame**, mean 184, 468 distinct over the window. Mix: 233 motorcycle / 119 car / 68 auto / 17 van / 15 bus / 15 truck / 1 ambulance. Perception issue — 184 vehicles spread over a 3.7 × 2.1 km network at overview zoom | Camera framing + vehicle visibility work (cycle 6) | n/a | ACCEPT |
| 3 | MAKER | Disturbance system (Part D) | Implemented `ems_sim/disturbance/incident.py`: lane closure via `setDisallowed` + speed drop, restored exactly. No vehicle moved, stopped or rerouted | — | ruff, focused | ACCEPT |
| 4 | REVIEWER | Audit disturbance v1 | **Two real defects.** (a) Incident had *zero* effect: placed on `92196679#0`, which carries ~26 vehicles / 360 s — no queue can form on a road that empty. (b) `getLastStepLength` was reported as queue length; it is SUMO's **mean vehicle length**, so "max queue 12 m" was a bus | — | — | REJECT |
| 5 | DEBUGGER | Fix both | (a) Relocated incident to the actionable signal's approach `1393474724#1` under a new, documented rule; rule changed on the flow measurement, **not** on any travel-time effect. (b) Queue length now from SUMO's own `--queue-output`; TraCI series keeps halting counts + occupancy only | | ruff, run | ACCEPT |
| 6 | REVIEWER | Re-check disturbance | Incident applies and clears; genuine queues form (**202 m network max, 48.6 m on route**). But ambulance travel time unchanged at 184.5 s. Measured why: **all three GS_cluster approaches carry only 20–30 vehicles / 360 s** at demand scale 0.5. No blockage near this signal can congest it | None — preserved as a finding rather than inflating demand to manufacture one | run | ACCEPT (negative result preserved) |
| 7 | MAKER | Vehicle assets (Part A) | Attempted external sourcing first: network access confirmed working (Khronos GLB downloaded, HTTP 200, valid binary). Searched the Khronos glTF-Sample-Assets index (150 models) | **No suitable set obtainable.** The only reachable vehicle models are rendering showcases — a milk truck, a toy car, a concept car — and there is no motorcycle, auto-rickshaw, bus or ambulance. Authored 7 silhouettes instead (wheels, glazing, 3-wheel auto, cab-plus-body truck), sized from the SUMO vTypes; built a GLB-capable registry so licensed assets drop in per type without code change | ruff, tsc | ACCEPT |
| 8 | REVIEWER | Audit assets | Not cubes; each type distinguishable at traffic distance. Dimensions traceable to vType. Fallback is **not** silent — `assetReport()` surfaces source per type in the HUD. Provenance file records the sourcing attempt and refusal | — | tsc | ACCEPT |
| 9 | MAKER | Integrity checks (Part I) | `scripts/check_integrity.py` — 15 checks per seed across the 4 paired runs: network/demand/seed/mix/route/OD/disturbance identity, only-policy-differs, no teleport, no artificial speed, signal states from SUMO, TLS present, completion, metrics from simulation | — | ruff | ACCEPT |
| 10 | MAKER | Disturbance UI + EMS status + demo (Parts E/M/N) | Disturbance panel (declared config + SUMO queue measurements, never zeros when absent); EMS status replayed from recorded transitions only; demo chapters anchored to recorded times, **struck through when the evidence is absent from the run** | — | tsc | ACCEPT |
| 11 | MAKER | Comparison mode (Part L) | Two panes, two exported runs of the same scenario, **one clock** — only the left pane advances it, so the views cannot drift | — | tsc | ACCEPT |
| 12 | TEST | Disturbance regression tests | 16 tests: window bounds, apply/clear once, exact restore, **never touches a vehicle**, emergency class blocked too, config-hash stability and sensitivity | — | 488 pytest, 24 vitest, ruff clean | ACCEPT |
| 13 | MAKER | Seed 42 with disturbance (Parts F/G) | 4 policy runs, identical scenario, incident applied in all four | — | integrity | ACCEPT |
| 14 | REVIEWER | Seed 42 gate (Part H) | **15/15 integrity checks PASS.** Same network/demand/seed/mix/route/OD/disturbance hash; only policy differs; 0 ambulance teleports; max observed ambulance speed within vType; 0 signal conflicts; ambulance completed in all four. Results: NORMAL 184.5 s → EMS 172.0 s, saved 12.5 s, route queue 48.6 m | Wired check 11 (no frontend-only traffic) to read the scene validator's result rather than leaving it unchecked | integrity, 488 pytest | ACCEPT |
| 15 | MAKER | Cross-seed reporter (Parts J/K/R) | `scripts/final_validation.py` — every run tabulated, cross-seed mean/median/sd/min/max, per-seed saving shown so nothing hides in a mean, intersection ranking by recoverable delay, traffic term flagged DIAGNOSTIC ONLY | — | ruff, smoke on seed 42 | ACCEPT |
| 16 | MAKER | Second asset-sourcing attempt (Part A addendum) | Time-boxed: 1 web search + 6 direct fetches. Khronos (no motorcycle/auto/bus/ambulance); Kenney+Quaternius CC0 mirror repo has **59 GLBs, 0 vehicles**; 3 candidate raw paths 404; GitHub code search 401; Poly Pizza needs a key | **Identified 3 genuinely CC0 packs by name** (Eclair/Kenney Car Kit, RgsDev Free Low Poly Vehicles, Quaternius) — all behind itch.io/site download flows, not fetchable unauthenticated. No CC0 auto-rickshaw found anywhere. Kept authored silhouettes; recorded the named packs so a human can fetch in one step; added explicit statement that these are **not** GLB assets | ruff | ACCEPT (documented failure) |
| 17 | MAKER | Honest results panel (Final Demonstration Requirement) | Paired comparison embedded at export time; HUD reads it rather than recomputing, so scene and metrics cannot disagree. **A zero prints as "0.0 s" in neutral type** — no green, no arrow, no "improvement" framing — with a line saying it is zero rather than a small benefit | — | tsc | ACCEPT |
| 18 | REVIEWER | Audit the 20 disturbance runs | **CRITICAL DEFECT.** All 5 seeds passed 15/15 integrity checks and the ambulance result looked clean — but the disturbance changed the ambulance's travel time by *exactly* 0.0 s in all 20 runs, and `vehicles arrived` was **byte-identical** with and without the incident in every seed. A lane closure in a 3,226-vehicle network cannot do nothing | — | — | **REJECT** |
| 19 | DEBUGGER | Find the cause | Measured per-lane usage: `1393474724#1_0` carries **0 vehicles** (all 28 use lane 1); `92196679#0_0` likewise 0 of 26. **Both incident placements blocked a lane no traffic uses.** Also recognised that closing the *only* used lane could sever the route, which would change the ambulance's trip rather than delay it | Lane chosen by measured flow, not assumed to be lane 0; blockage changed to a *partial obstruction* (lane stays open, speed drops to a crawl) so connectivity is preserved | 16 disturbance tests | ACCEPT |
| 20 | REVIEWER | Re-check the fixed disturbance | Seed 42 NORMAL: ambulance **184.5 → 282.5 s (+98 s)**, waiting 5.5 → 53.5 s, stops 1 → 2, route queue 76.8 m, 18 vehicles halted, network arrived 2897 → 2892. The disturbance is now genuinely biting. Nothing was chosen against the ambulance's travel time — lane by vehicle count, blockage type by connectivity | 19 invalid runs archived as `_unused_lane_blockage` with a README explaining they are evidence *for* the defect | — | ACCEPT — matrix relaunched |
| 21 | REVIEWER | Audit scene exports | **Two defects.** (a) Both incident exports wrote to the same directory, so the EMS export silently replaced the NORMAL one — compare mode would have shown one run twice. (b) Both were produced before the disturbance fix, so they carried the inert incident | Added `--out` so each export names its own subdirectory; deleted the clobbered output; requeued both behind the corrected matrix | ruff | ACCEPT |
| 22 | MAKER | Attribution panel (Part K UI) | Per-signal recoverable delay exported into the manifest and rendered as a ranked bar list. A signal passed on green shows **0.0 s** and says why; the unattributed residual is shown separately, not folded into the total | — | tsc | ACCEPT |
| 23 | MAKER | Teleport surfacing + queue tests | Network teleports now reported **per policy** in the integrity output, with a note when a policy exceeds NORMAL — a teleported vehicle's travel time is not a time anyone drove, and it hides inside the traffic aggregate. 6 tests on `queue_summary`, including that it refuses to report a recovery time when there was no incident | — | 494 pytest | ACCEPT |
| 24 | MAKER | Interpretation framework | `docs/INTERPRETATION_FRAMEWORK.md` separates OBSERVED / ASSUMED / INTERPRETED, tabulates every assumption with what it would invalidate, and lists the claims that are explicitly not made | — | — | ACCEPT |
| 25 | MAKER | Trade-off + robustness analysis | `scripts/policy_tradeoff.py` — paired differences per seed (travel/waiting/time-loss/stops/traffic/teleports/queue/recovery), ordering robustness per seed, |z|>2 outlier detection, disturbance config-hash consistency, per-intersection consistency across seeds | — | ruff | ACCEPT |
| 26 | REVIEWER | Interim read, seeds 42–44 | **Ordering holds 3/3.** `FULL − ROLLING = +1.5 s` in *every* seed. **Teleports rise monotonically with preemption strength**: NEXT −0.33, ROLLING +0.67, FULL +2.33. Traffic Δ means rise too (−5,336 / +16,293 / +26,106) but sd exceeds or rivals mean, consistent with the established no-stable-sign finding. `GS_cluster` accounts for all recoverable delay (nonzero 9/9); `joinedS_` exactly 0.00 in 0/9. No outliers | — | — | ACCEPT (interim, awaiting 45–46) |
| 27 | REVIEWER | Adversarial disproof harness | `scripts/adversarial_review.py` — 9 probes that try to *break* the result rather than confirm it: paired-scenario identity, route identity, artificial speed, ambulance teleport, disturbance identity, **disturbance-is-not-inert** (the probe that would have caught cycle 18's defect), leave-one-out ordering robustness, frontend-only effects, signal states outside program. Seeds 42–44: **9/9 SURVIVED** | — | ruff | ACCEPT (interim) |
| 28 | REVIEWER | Full 5-seed analysis | **Ordering holds 5/5**, leave-one-out clean, no |z|>2 outliers, disturbance single-hash across all 20 runs. Adversarial: **9/9 SURVIVED**. Integrity: **PASS on all 5 seeds** (16 checks) | — | — | ACCEPT |
| 29 | REVIEWER | Investigate the NEXT-vs-ROLLING gap | Waiting-saved is *identical* across all three policies within every seed, yet ROLLING saves ~14 s more than NEXT. Traced it: at the signal the policies are near-equal (47.7/48.9/48.9 s attributed); the gap lives on the **964 m upstream edge** (seed 42: 84.5 → 68.5 NEXT vs 54.5 ROLLING) and surfaces as *unattributed* residual (16.5 vs 29.5 s) | Documented as a **methodological limitation**: attribution credits only the approach edge, so it under-credits a policy whose benefit is upstream queue discharge. Residual still correctly not assigned to any intersection | — | ACCEPT |
| 30 | MAKER | Final report | `docs/FINAL_RESULTS.md` — 10 sections, every claim tagged OBSERVED / ASSUMED / INTERPRETED, per-seed values shown so nothing hides in a mean | — | — | ACCEPT |
| 31 | REVIEWER | Final gate after scene re-export | **Scene validation FAILED — 375 failures.** Not a scene defect: the validator *reconstructed* the FCD path from demand id + policy, so once incident exports introduced their own tag it silently compared the new scene against a different run's recording | Export now records `fcd_file` in the manifest; validator uses the recorded path instead of rebuilding it. Re-validated: **577 checks, 0 failures** | scene validation | ACCEPT |
| 32 | REVIEWER | Complete final gate | Integrity **PASS 5/5** (16–17 checks). Adversarial **9/9 SURVIVED**. Scene validation **577/0**. Demo cross-check: primary NORMAL 282.5 s / 53.5 s wait / 2 halts; compare EMS_NEXT 220.0 s / 0 s wait / **62.5 s saved** — identical to seed 42's research record, same incident hash, same seed | — | 494 pytest, 24 vitest, ruff clean, tsc clean | **ACCEPT — gate passed** |
| 33 | REVIEWER | Read-only publication audit | 15-point cross-check of every headline number against the authoritative JSON. **Means, savings, ordering, run count, demo numbers, percentages and the 98.8% figure all verified exact.** Three documentation defects found, none affecting the experiment: (a) four standard deviations in FINAL_RESULTS §3 were hand-written and wrong (waiting 2.36→2.31; time-loss 3.14→3.20, 2.13→2.14, 2.11→2.20); (b) the disturbance range "98–102 s" understated the low end (actual 97.5); (c) the report tagged 16 OBSERVED and 8 INTERPRETED statements but only **1 ASSUMED**, naming no assumption inline | Corrected the four sd values from the JSON, corrected the range to 97.5–102.0 with per-seed values, added §9b tabulating every assumption with its data class and why it matters. **No simulation, parameter or result changed** | read-only + docs | ACCEPT |
| 34 | MAKER | Real traffic data investigation | TomTom 401 (licensed); data.gov.in no resource; police/B-SMILE PDFs only. One GitHub CSV with 275 "Silk Board Junction" rows fetched | — | — | ACCEPT |
| 35 | REVIEWER | Audit the CSV before use | **Synthetic.** Weekday/weekend volume ratio 0.984 (no weekly structure), 81.9 km/h average speed at Silk Board, values clipped at exactly 100.0, 14-decimal floats, no licence, no source | Rejected; nothing ingested. Investigation recorded in `data/traffic/provenance/`. Demand stays ESTIMATED_DATA | — | ACCEPT (negative) |
| 36 | MAKER | Demo/research separation, denser demand, 4-incident set, arrival stop | Demo scale 0.8 (below the 1.0 gridlock point); `IncidentSet` with 4 obstructions on measured-trafficked lanes; runner breaks on SUMO arrival when opted in | All demo behaviour opt-in, defaults unchanged for research | 494 pytest | ACCEPT |
| 37 | REVIEWER | Audit demo export | **Four defects.** (a) demo HUD showed the **research** seed-42 saving (78.5 s) against demo traffic — cross-experiment contamination; (b) trajectory window ended at 900 s, ambulance arrives at 1,093.5 s, so the arrival was never visible; (c) `mode` field never reached the manifest; (d) no queue output | Demo compares against its own paired NORMAL run (133.0 s), labelled NOT the research experiment; window extends to arrival + 12 s; mode fixed; queue output added | re-export | ACCEPT |
| 38 | REVIEWER | Scene validation, dense demo | **5 continuity failures in 471,936 samples**, all the same junction: vehicles moving 6.8 m in 0.5 s at ~1 m/s. 3.1 m of road + 6 m of spurious climb. `edge_layer()` gave internal connectors the **max** layer at the node, lifting street-level connectors to deck height | Connector layer now the min of the edges it actually joins | 0 continuity failures | ACCEPT |
| 39 | REVIEWER | Heading check, EMS scene | 1 failure: ambulance turning 27° in 0.5 s inside joinedS_. Endpoint-mean heading assumes a uniform turn | Derived bound: chord of a monotone turn lies between endpoint headings, so error ≤ half the swept angle. Mirror still caught by forward_dot and straight-lane samples | 577/0 | ACCEPT |
| 40 | REVIEWER | Signal rendering in an EMS run | **Critical.** Frontend rebuilt lamp colours from the static program. Correct for NORMAL, **wrong for EMS**: the policy holds/truncates phases, so the green corridor would never appear on the lamps | Runner records applied TLS state from TraCI on change (opt-in, read-only); renderer replays it; program reconstruction is fallback only. Demo re-simulated to record it | 33 vitest incl. replay + arrival-stop tests | ACCEPT (pending re-run check) |
| 41 | REVIEWER | Re-check after demo re-simulation | **Deterministic**: EMS_ROLLING 360.0 s / 0.0 s / 0 stops and NORMAL 493.0 s / 125.0 s / 4 stops reproduce exactly. Applied signal states recorded for **8/8** TLS (288 changes). During every recorded PRIORITY_ACTIVE episode the **ambulance's own controlled links are green in 12/12 probes and other movements are held red in 12/12** — in the state SUMO applied, not a reconstruction. Scene validation 577/0 | — | 494 pytest, 33 vitest, ruff, tsc | ACCEPT |
| 42 | REVIEWER | Browser end-to-end walkthrough (scripted, live dev server) | HUD renders; **no server or console errors**. Status read from the page at recorded times: t=595 "Monitoring", t=602.2 "Priority active" (joinedS_), t=642.5 "Priority active" (GS_cluster). Played from t=958: clock **clamped to exactly 960.5 s** (SUMO arrival), play button flipped to paused, status "Arrived", still 960.5 s after a further 2.5 s. Result card shows 360.0 s / 0.0 s / 0 stops / **133.0 s** / EMS_ROLLING, matching the run record. **Defect:** on a narrow pane the result card clipped at both edges and the corner disclaimer overlapped its Replay button | Card width capped to the viewport; corner notice hidden while the card is shown; the card now carries its own simulated-data notice | tsc, vitest | ACCEPT (layout re-check pending) |
| 43 | REVIEWER | Re-check result card layout | Card fully inside a 291 px CSS viewport (left 16, right 275); corner notice hidden while the card is shown; card carries its own "simulated demo scenario — not the research result" notice; no overlap. All gates green: 494 pytest, 33 vitest, tsc clean, ruff clean, scene validation 577/0, frozen research verified intact read-only. **Not committed** | — | browser, tsc, vitest | **ACCEPT — phase complete** |

## HISTORICAL_DEMO phase

Frozen research untouched throughout. Details are in [`HISTORICAL_DEMO.md`](../HISTORICAL_DEMO.md).

| # | Role | Task | Finding | Change | Checks | Verdict |
|---|---|---|---|---|---|---|
| 44 | MAKER | Historical source investigation | **CMP 2019 draft, Table 2-13**, Silk Board Junction (4-legged): peak hour **22,634 veh / 18,180 PCU**; 24 h **323,099 veh / 281,521 PCU**. **RMP-2031 Vol. 3** dates its 14-location 24 h turning counts to **Dec 2014 – Apr 2015**. **IJIRSET June 2017**: cars 53%; existing cycle 450 s | OBSERVED values only in `observed_counts.json` / `.csv`; every source, used or not, in `provenance.json` | — | ACCEPT |
| 45 | REVIEWER | Audit sources before use | Rejected as substitutes: the BBMP tunnel DPR (Aug 2024) has no junction count (nearest is a mid-block count on NH-44 near Madiwala), and the B-SMILE count is on the Indiranagar–Domlur road. The 53% sits inside a left-turn recommendation (scope unclear) and comes from a different survey than the volume. PCU per vehicle (0.803) cannot be inverted without the survey's PCU factors | Car share kept OBSERVED with a scope caveat; applying it to the 2014–15 volume labelled ESTIMATED; no class table manufactured; the loader asserts unit strings so PCU can never be read as vehicles | pytest | ACCEPT |
| 46 | MAKER | Four-way network variant | Research network: Silk Board is 4 independent ~90%-green approach signals. A `--tls.join-dist 80` attempt joined the wrong signals | Demo-only variant `silk_board_v1_hdemo`: the 4 approach nodes under one TLS `silk_board_4way`. Split-phase program: cycle 450 s OBSERVED; split, amber, all-red and order ESTIMATED. 12 build checks, including research network byte-identical | build checks | ACCEPT |
| 47 | REVIEWER | Phase order vs documentation | Docs said "clockwise from N" but E sorted first, because the N arm's bearing is 354.8° | Bearings rotated 45° before sorting; rebuilt; N→E→S→W verified | pytest | ACCEPT |
| 48 | MAKER | Trip rule and demand scale | Trip rule fixed before any run, duarouter only: 90 pairs, 41 use the four-way → `1534247773 → 172853384#3` (1,742.7 m, 3 red-exposed signals). Scale grid fixed before any run; NORMAL-only sweep: all 4 k valid (0 teleports, 0 backlog) → **k = 0.25** | — | sweep | ACCEPT |
| 49 | REVIEWER | Audit the sweep | The selected k is the grid's upper bound, not a capacity limit. No EMS run and no travel time informed it | Recorded as a limitation: the demo uses 25% of the observed peak hour | — | ACCEPT |
| 50 | MAKER | Export and ramped elevation | `--historical` export: own namespace, no disturbance, stop at arrival, time-to-teleport 600 s (ESTIMATED). Opt-in ramped elevation: structure ends meet their road over 120 m, junction lanes interpolate along `via` chains; the flat rule is unchanged for every other export | — | scene validation 0 failures (779,039 + 283,635 samples) | ACCEPT |
| 51 | REVIEWER | Audit recorded priority at the four-way | The policy records CLEARING→NORMAL→REQUESTED whenever the ambulance crosses a junction interior (no route index) while the applied state is unchanged, so a literal banner flickers. The ambulance's arm was already green at the four-way: priority **held it to 676.0 s vs the programmed 670.5 s**, then amber → all-red → S green. Truncate-and-transition appears at GS_cluster (cross green cut after 5 s; ambulance green at 734.5 s) | Display rule: re-entries within 3 s form one episode; ACTIVE only when PRIORITY_ACTIVE was recorded **and** the applied ambulance movement is green; raw transitions untouched. The trip was **not** re-chosen for a more dramatic transition | vitest | ACCEPT |
| 52 | REVIEWER | Delay-marker labels | A halt on amber was labelled "signal was green" | The label names the recorded own-link state (red / amber / green with queue) | tsc | ACCEPT |
| 53 | REVIEWER | Browser walkthrough 1 | Heads at 5.4 m cut into the flyover soffit (4.9 m) over the at-grade junction. The banner showed a raw controller id. The intersection camera was too far out to see the heads | Heads at 3.6 m; approach road names exported; the intersection camera sits behind the ambulance's own approach | browser | ACCEPT |
| 54 | REVIEWER | Validation 21/23 | C19 failure was a validator bug: the all-red state was looked up by first match, but it occurs 4 times in the program. C20 failed only on duarouter's timestamped header; every simulated quantity reproduced | Validator walks the program from the first unique state; routes compared without the timestamp header. Rerun **23/23** | validate | ACCEPT |
| 55 | REVIEWER | Browser walkthrough 2 | COMPARE: window 540–1132.5 s, stop at 1120.5 s. At 8× from 770 s, the EMS pane froze on "ARRIVED · 177.5 s" while NORMAL continued. From 1112 s the clock clamped at **exactly 1120.5 s** and held; panel showed **520.0 / 177.5 / 342.5 s**. SINGLE restored stop at 778 s. No console errors. Defects: the priority banner covered the right pane's label; the snapshot card covered the EMS pane; buildings rendered near-white; delay markers showed before their halt | Banner drops below pane labels in COMPARE; snapshot collapses in COMPARE; building tones darkened; markers appear from their recorded time | browser, tsc, vitest | ACCEPT |
| 56 | REVIEWER | Final gate | `validate_historical_demo.py` **23/23**, including the SUMO rerun. Full pytest **519 passed**; vitest **49 passed**; tsc and ruff clean; scene validation PASS with 0 failures for both runs. Frozen research: **178 files byte-identical, 0 added**. Walkthrough 3: at t = 649 s in COMPARE, the NORMAL pane shows no marker before its halt; the EMS pane shows the corridor, heads, stop bar and "cross traffic held at red"; the snapshot toggles. The flyover overview shows ramps meeting the ground. The only console errors were from a transient hot-reload state mid-edit; after reload the components they named render and work. **Not committed** | — | all | **ACCEPT — phase complete** |
| 57 | REVIEWER | The block over the intersection | The grey slab filling the intersection view is OSM way **1508372362** (`building=construction`, `construction=train_station`) — the metro station box. Its footprint covers **596 m of live carriageway**, including `380980308#2`, the ambulance's own approach. Two station footprints do the same on the flyover roads. Buildings have no vertical placement in this project, so all of them are extruded from the ground: the structure is drawn *through* the road it stands over | Rule: a footprint covering at least 20 m of drivable lane centreline is not drawn. 16 of 2,500, each listed with its overlap in `buildings.json`; the other 2,484 are untouched. Not a camera move, and nothing replaces them | pytest | ACCEPT |
| 58 | REVIEWER | The ambulance was leading the queue | In NORMAL the ambulance reached the four-way stop line with **0 vehicles in front of it** and waited out the red alone. Nothing about that shows what priority is worth | The scenario is now chosen by `scripts/historical_scenario_sweep.py`: demand scale **and** departure together, from NORMAL runs only, requiring the ambulance to halt behind at least 12 vehicles at the four-way. The runner records, at every halt, how much traffic stands between the ambulance and the next signal (opt-in; research runs unchanged) | sweep | ACCEPT |
| 59 | MAKER | Priority that asks in advance | A fixed 250 m or 700 m trigger is a distance, not a lead time, and knows nothing about the controller's transition or the queue | Demo-only `EMS_PREDICTIVE`: asks when `eta(distance, speed) <= transition + queue clearance + margin`, where the transition is computed from the controller's own program and the clearance from SUMO's halting count — so `activation_distance = f(speed, distance, transition)`. Holds up to 180 s so an early request survives to arrival. Selects among program phases only, like every policy here. The research policies are untouched | pytest, ruff | ACCEPT |
| 60 | REVIEWER | Audit the first scenario rule | Searching the scale grid upward picked the **lightest** demand that just cleared the bar: 10 vehicles packed into the last 19 m, with the rest of the network nearly empty. The 60 m "not at the head" test was also wrong for this geometry — the approach edge is only 58 m long, so it demanded the queue spill past the junction upstream | Rule reversed to search from the densest end (a larger k is a larger share of the observed hour), the queue requirement raised to 12 vehicles, and the distance test reduced to "not halted on the line". k = 0.40 then produced a 30-vehicle queue but **failed validity on teleports and insertion backlog**, and the rule rejected it — which is the "vehicles must not teleport away" requirement doing its work | sweep | ACCEPT |
| 61 | MAKER | Make the state obvious | Signal heads read as dots from the intersection camera | Each approach's stop bar now carries that approach's own recorded aspect, and shows neutral when its heads disagree rather than inventing one colour. The intersection camera is computed by the exporter from the controller's stop-line geometry — 130 m back down the ambulance's approach, 38 m to the side, 46 m up — so the queue and the junction are in one diagonal frame, and the same definition is what the validator checks the line of sight against | tsc, 52 vitest | ACCEPT |
| 62 | REVIEWER | Scene and validator defects found by the new runs | Three. (a) At this demand a 0.5 s recording is ~100 MB per scene and the comparison parses two. (b) The scene validator failed one continuity sample: a car standing in a queue appeared 6.4 m sideways with both samples reading zero speed — SUMO's own discrete lane change, not a transform error, but the allowance had no term for it. (c) The determinism rerun deliberately runs 600 s past arrival, and jams *after* the demo window teleported vehicles, failing a check about the demo | (a) HISTORICAL_DEMO records at 1.0 s — an output setting that changes no simulation — giving 16 MB per scene. (b) Continuity now allows the width of the lanes actually crossed **within the same edge**; an edge change still gets nothing, so a teleport stays caught. (c) The rerun's teleport and conflict checks are scoped to the demo window; the exported runs' own counts are check C28 | scene validation, validate | ACCEPT |
| 63 | REVIEWER | Final gate | Scenario sweep chose **k = 0.30, depart 700 s** — 17 vehicles between the ambulance and the stop line, 0 teleports, 1.9% backlog — after rejecting k = 0.40 and 0.35, whose bigger queues came with teleports. Runs: NORMAL **441.5 s** (262.5 s waiting, 18 stops) vs EMS_PREDICTIVE **193.5 s** (51.5 s, 2 stops), **248.0 s saved**. Priority asked at t=700.5 s, **1,004 m out**; green at 726.5 s with the ambulance still 506 m away; **13 vehicles ahead at green, 0 at the stop line**. Validation **28/28** with the SUMO rerun; 526 pytest; 52 vitest; tsc and ruff clean; both scenes 0 scene-validation failures; frozen research **178 files unchanged, 0 added**. Browser walkthrough: intersection view clear of the station box, 8× playback, each pane frozen at its own arrival, clock clamped at 1142.0 s, panel 441.5 / 193.5 / 248.0 s, no console errors. **Not committed** | — | all | **ACCEPT — phase complete** |
| 64 | REVIEWER | The ambulance was drawn through the traffic | **The recorded point is the front bumper, and the renderer centred the model on it.** Proved from a stopped queue in the recording, where the distance between consecutive recorded points is exactly the leader's length plus the follower's `minGap`: bus→car 7.00 m, car→motorcycle 4.20 m, motorcycle→motorcycle 3.00 m. Every body was therefore drawn half its own length too far forward — worst for the 6 m ambulance, whose drawn nose sat ~0.1–0.3 m from the small vehicles ahead instead of the 2.0 m gap SUMO had actually left. Positions, headings and the transform were all correct | `bodyCentreOffset()` in `coords.ts`, applied identically to every instanced vehicle, the ambulance and the selection ring. **No ambulance-only offset** | 3 new vitest cases (offset direction, no lateral component, restores exactly the 2.0 m minGap); 55 vitest | ACCEPT |
| 65 | REVIEWER | The ambulance drove through two buildings | OSM footprints `347584721` (516 m², worst penetration **2.20 m**) and `347584061` (136 m², 2.05 m) overlap the carriageway of `688824218#1` — the lane the ambulance used — by less than the 20 m of centreline the drop rule needed, so they were drawn. 533 footprints in the scene touch a carriageway at all; dropping them all would delete a fifth of the city | Footprints are **trimmed**, not dropped: the drivable surface (each lane at its own width + 0.75 m, plus the junction shapes) is subtracted from every footprint. 915 of 2,484 lose some area, 15,342 m² in total, **0 lose all of it**. The margin is vehicle geometry — the lane centreline plus the widest body's half-width plus a corner allowance — and is documented as such | pytest (a wall on the road is trimmed to the lane edge; a building beside it is untouched, byte for byte) | ACCEPT |
| 66 | MAKER → REVIEWER | The ambulance waited for the cars during EMS priority | Signal priority clears the signal, not the queue, so SUMO's own emergency-yielding mechanism — the **bluelight device** — was implemented: command line only (`--device.bluelight.explicit`, routes file unchanged between the paired runs), named rather than sampled, EMS runs only. Then it was measured on the demo's own scenario against NORMAL's 532.0 s / 308.5 s waiting: **priority alone 255.0 s / 24.5 s; + siren at SUMO's default 200 m, the ambulance never arrives** — it stood at the same metre for **3,094 s**, on an unsignalised junction with an empty road ahead of the queue; **+ siren at 200 m with the sublane model** (0.8 m, so a car can sit to one side of its lane) **also never arrives**, 3,118 s at one metre; + siren at 30 m arrives in **815.0 s / 592.5 s**, worse than NORMAL. The device does what it promises and there is nowhere in a saturated three-lane corridor for the traffic to go: vehicles stop to let the ambulance through, it cannot pass stopped vehicles filling every lane, and neither side moves again | The demo ships **signal priority alone**. The device and the sublane switch stay in `demo_run_options`, both defaulting to off, with the measurements in the docstring so the finding is reproducible (`siren=True` restores the deadlock). C32 now checks the EMS run buys **no** SUMO behaviour NORMAL lacks, and C08 keeps the stronger bound: identical vehicle by vehicle until the policy first touches a signal | 4 SUMO runs | ACCEPT (negative) |
| 67 | REVIEWER | One overlap survived the bumper fix, and it was the route's own geometry | After the fix, 457 frames of NORMAL held exactly one drawn overlap: an auto 1.57 m inside the ambulance at t=696. SUMO was right and the scene was right — the two were **8.18 m apart along the lane path** and 2.97 m apart on the ground, because the connection between their edges **reverses direction by 159° in 4.88 m**. netconvert builds these at acute OSM intersections; no 6 m body could drive it. The 3 m centreline proxy the trip rule used could not see it (both edges were on the route), and it was also rejecting 39 of 41 routes for pairs 0.01–0.84 m apart, leaving the rule unsatisfiable | Trip rule now measures **the drawn geometry itself**, with the renderer's body placement and the validator's overlap measure: a route is dropped if a legally spaced body could be drawn inside the ambulance anywhere on its own path (8 of 41 rejected), and ranked by how many of its metres share ground with another road at the same level. Two roads on one piece of ground cannot be avoided in this extract — every route through the four-way has some — so it is minimised and then **checked frame by frame in the recording** (C29/C30). Also fixed: reading the net with internals made `getEdges()` return junction lanes, which turned every route into a conflict | pytest (straight path clean, hairpin caught, road on the same ground caught, road alongside or above not) | ACCEPT |
| 68 | REVIEWER | Final gate after the geometry and scenario rework | Trip reselected by the new rule: **`312063814#2 → 1534247766#0`**, 2,220.8 m, 8 of 41 four-way routes rejected as undrivable, and 1 metre of the chosen route shares ground with another road — the least of any candidate. Sweep: 16 pairs rejected, **k = 0.25 at 700 s** taken with **37 vehicles** between the ambulance and the stop line, its first halt **219 m** back, 0 teleports, 0.8% backlog. Runs: NORMAL **532.0 s** (308.5 s waiting, 309.0 s standing still, 5 stops) vs EMS_PREDICTIVE **255.0 s** (24.5 s, 25.0 s, 3 stops), **277.0 s saved (52.1%)**. Priority asked at 793.5 s, **548 m out at 11.4 m/s**, against an activation distance of **549.3 m** its own formula produced — the first metre at which it could ask; green at 819.5 s with **43 vehicles** still ahead; **0** ahead when it crossed at 881.0 s on `rrrrGGrrr`. Elsewhere in the city **181** vehicles stood still at that instant against **174** in NORMAL. Validation **33/33** including the SUMO rerun; **0 ambulance↔building intersections and 0 ambulance↔vehicle overlaps in both runs** at a 5 cm numerical tolerance; 532 pytest; 55 vitest; tsc and ruff clean; both scenes 0 scene-validation failures (208,382 and 428,777 vehicle samples); frozen research byte-identical. Browser: intersection and chase views show clear spacing, COMPARE freezes each pane at its own arrival and reads 532.0 / 255.0 / 277.0, no console errors | Fixed in passing: the heading check counted a lane change as a heading error (the same false positive the continuity check had in cycle 62) and now gets the same same-edge allowance | all | **ACCEPT — phase complete. Not committed** |
