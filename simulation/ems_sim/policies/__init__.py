"""Traffic-signal policies.

A policy is the *only* thing allowed to differ between a baseline run and its
counterfactual. Everything else — network, demand, seed, ambulance trip — is held
constant, which is what makes the difference in travel time attributable.
"""
