"""Simulation state frames.

Left empty on purpose. The frame schema should be derived from what the TraCI
loop actually reads out of SUMO, not designed in advance — a schema invented
first tends to acquire fields that get filled with plausible values rather than
measured ones.
"""
