"""HISTORICAL_DEMO: a SUMO replay driven by historical observed Silk Board counts.

A presentation mode, separate from the frozen research experiment. It reads its
observed inputs from ``data/traffic/historical_central_silk_board/``, runs on its
own network variant and writes to its own output namespace. Nothing here reads or
writes a research output.
"""
