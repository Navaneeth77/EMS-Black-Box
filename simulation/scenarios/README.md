# simulation/scenarios/

Committed scenario definitions: the `ScenarioConfig` JSON files that, with a
seed, reproduce a run exactly.

These are inputs and belong in git. The SUMO artefacts they generate live in
`simulation/sumo/` and do not.

Empty until Phase 1 produces a network to point at.
