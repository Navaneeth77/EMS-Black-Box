# Shared development tooling for backend, simulation and analysis.
# Install into the same virtualenv as the component requirements files:
#   pip install -e ".[backend,dev]"
#
# Read by pyproject.toml through `[tool.setuptools.dynamic]`, so comments stay
# on their own line.

pytest>=8.0
pytest-asyncio>=0.24
# FastAPI TestClient transport
httpx>=0.27
# lint + format
ruff>=0.6
